/**
 * CovomoRechner — DEPRECATED 2026-04-29
 *
 * Das Covomo-iframe-Widget wurde abgeschaltet, weil wir SEO/GEO und
 * Lead-Tracking selbst halten wollen. Stattdessen wird der eigene
 * `TarifRechner` (gespeist aus der `tarife`-Tabelle) verwendet.
 *
 * Diese Datei bleibt nur als historische Notiz und wird in einem späteren
 * Cleanup-Commit komplett entfernt.
 */
export function CovomoRechner(): null {
  return null
}
